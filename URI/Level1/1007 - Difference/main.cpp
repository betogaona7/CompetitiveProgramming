#include <stdio.h>

using namespace std;

int main()
{
    int a, b, c, d, diference;
    scanf("%i %i %i %i", &a, &b, &c, &d);
    diference = (a*b)-(c*d);

    printf("DIFERENCA = %d\n", diference);
    return 0;
}
